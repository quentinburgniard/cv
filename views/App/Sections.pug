div(class="row" v-for="section in " + sections)
  div(class="col-12 col-sm-6")
    input(type="text" placeholder=__("Title") v-model.lazy.trim="section.title")
  div(class="col-12")
    textarea(placeholder=__("Description") v-model.lazy.trim="section.text")
div(class="row")
  div(class="col-12 col-sm-10")
    div(class="row")
      div(class="col-12 col-sm-6")
        input(type="text" placeholder=__("Title") v-model.lazy.trim="draft." + events.split('.')[2] + ".title")
      div(class="col-12")
        textarea(placeholder=__("Description") v-model.lazy.trim="draft." + events.split('.')[2] + ".description")
  div(class="col-12 col-sm-2")
    <button type="button">+</button>